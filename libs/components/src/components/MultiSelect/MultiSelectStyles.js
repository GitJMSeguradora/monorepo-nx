import styled from 'styled-components';
import { SelectCreatable } from 'junto-ui';
import { default as TagButtonComponent } from '../TagButton';

export const Container = styled.div``;
export const TagsContainer = styled.div``;
export const Select = styled(SelectCreatable)``;
export const TagButton = styled(TagButtonComponent)`
  margin: 8px 8px 0 0;
`;

export const FixedItem = styled.div``;

export const FixedItemText = styled.div``;
